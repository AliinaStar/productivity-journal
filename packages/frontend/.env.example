# URL of the backend API server
# In development, this is usually your local machine's IP (not localhost, since
# the mobile app runs on a physical device or emulator on the same network)
EXPO_PUBLIC_API_URL=http://192.168.x.x:8000

# Set to "development" to skip OTP and use the dev-login shortcut
EXPO_PUBLIC_APP_ENV=development
