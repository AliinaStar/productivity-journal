# Secrets must never be baked into image layers — they are injected at
# runtime via `env_file` in docker-compose.
.env

# Local-only artifacts that bloat the build context.
.venv
.pytest_cache
__pycache__
*.pyc
notebooks
tests
.expo
